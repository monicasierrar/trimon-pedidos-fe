// src/config.tsx

// 🚩 Cambio: se define una bandera global para controlar si la app requiere autenticación o no
export const USE_AUTH = true; 
// 👉 Cambia a true para habilitar el login con Zoho y validar el session_token
// 👉 Déjalo en false para que todo cargue sin autenticación (modo libre)
